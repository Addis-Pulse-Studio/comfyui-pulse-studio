__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
.DS_Store

# GPL-3.0 reference material, reviewed for ideas only. Kept out of the repo so it
# can never be committed, packaged, or redistributed alongside this MIT project --
# shipping it would create exactly the licensing entanglement the fork avoids.
reference_purpose only/

# The planning document is local working material, not part of the node.
H3_Omni_Director_Blueprint_v2.md
